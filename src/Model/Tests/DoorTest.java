package Model.Tests;

import Model.Door;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class DoorTest {
/*
    //Changed Door constructor visibility from package to public to test
    @Test
    public void testDoorConstructor() {
        Door door = new Door();
        assertTrue(door.wasFirstTime());
        assertNotNull(door.getQuestion());

    }

    //Changed Door constructor visibility from package to public to test
    //Changed myLocked field visibility from private to public to test
    @Test
    public void testIsLocked() {
        Door door = new Door();
        assertEquals(door.myLocked, door.isLocked());
    }

    //Changed Door constructor visibility from package to public to test
    //Changed myFirstTime field visibility from private to public to test
    @Test
    public void testWasFirstTime() {
        Door door = new Door();
        assertEquals(door.myFirstTime, door.wasFirstTime());
    }

    //Changed Door constructor visibility from package to public to test
    @Test
    public void testSetFirstTime() {
        Door door = new Door();
        door.setFirstTime(false);
        assertFalse(door.wasFirstTime());
    }

    //Changed Door constructor visibility from package to public to test
    @Test
    public void testLockDoor() {
        Door door = new Door();
        door.lockDoor();
        assertTrue(door.isLocked());
    }

    //Changed Door constructor visibility from package to public to test
    //Changed myQuestionAnswer field visibility from private to public to test
    @Test
    public void testGetQuestion() {
        Door door = new Door();
        assertEquals(door.myQuestionAnswer, door.getQuestion());
    }*/
}
